export interface TravelPackage {
  id: string;
  title: string;
  image: string;
  duration: string;
  rating: number;
  price: number;
  featured: boolean;
  trending: boolean;
  description: string;
}
